$(document).ready(function(){  
    $(".button-collapse").sideNav();

    /*$(window).scroll(function() {
        scrollTop = $(window).scrollTop();
        console.log('kek');
        
        if (scrollTop >= 55) {
            $('.menu-button').css('color', '#29283f')
        } else {
            $('.menu-button').css('color', 'white')
        }
    }); */
});